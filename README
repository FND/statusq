StatusQ
microblogging for small/medium teams


Dependencies
============

* Python v2.5+
* Redis
* pip (`sudo easy_install pip`)
* virtualenv (`sudo pip install virtualenv`)


Getting Started
===============

    $ make init
    $ source activate
    $ make server
    # visit http://127.0.0.1:5000
